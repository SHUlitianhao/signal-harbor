# 项目默认规则

- 本项目允许位于 Windows 目录或 WSL/Linux 目录中；后续默认以 `WSL/Linux 文件系统中的项目目录` 作为主要开发位置。
- 默认优先将 `代码仓库、文档、虚拟环境、依赖目录` 放在 `WSL/Linux 文件系统` 中，以获得更好的 `git / Python / Node / Docker / Codex CLI` 兼容性与性能。
- 运行数据、抓取原始文件、日志、模型缓存和大体积媒体文件可放在 Windows 非系统盘的数据目录中，并通过配置路径或挂载路径引用。
- 默认避免硬编码本机绝对路径；路径设计优先使用可配置项、环境变量或相对路径，兼容 Windows 与 WSL。
- 本项目的规范文档、产品文档、总体方案和设计说明默认使用中文编写。
- 本项目当前定位为“本机自托管投资情报收集器”，默认面向单用户、Windows 本机常驻和 Android 手机浏览器访问场景。
- 在没有额外说明时，产品规划优先采用“模块化、可插拔、渐进扩展”的方案，先保证基础链路稳定，再逐步扩展信息源和分析能力。
- 后续 agent 默认先读取 `docs/CURRENT_STATE.md` 获取当前节点状态；需要细节时再读取 README、PRD、架构和路线图。
- 默认检索和读取时排除 `data/`、`logs/`、`__pycache__/`、`docs/archive/`，优先遵守 `.rgignore`，避免运行数据、缓存和历史归档消耗上下文。
- 当前阶段已完成 RSSHub 首批公开 route 接入和同事件规则归并；后续扩展仍应保持 SourceAdapter 可插拔边界，不要把 RSSHub 的 Node/TypeScript 依赖并入 Signal Harbor Python 后端。
- 与用户的默认协作分工是：由对话中的 AI 负责方案、拆解、评审与文档沉淀，由 `Codex CLI` 负责仓库内代码实现、验证和迭代修改。
