# Signal Harbor 路线图

本文件只保留当前阶段判断和下一步任务。历史执行细节不再展开，避免后续 agent 反复读取长 goal 文本；当前状态先读 `docs/CURRENT_STATE.md`。

## 1. 当前节点

已完成可运行 MVP：

- Python 标准库后端、SQLite、本地 REST API、静态 PWA。
- SourceAdapter：fixture、RSS、RSSHub、JSON、低风险静态 HTML。
- RSSHub 首批真实 route：华尔街见闻重要快讯、财联社加红电报、金十重要快讯、同花顺重要要闻、DW 中文国际新闻。
- 采集、标准化、源级过滤、canonical_hash 去重、TaskRun、失败隔离。
- 规则摘要、实体/风险词提取、规则评分、证据引用。
- FTS5 优先、LIKE fallback 的搜索和多条件过滤。
- 收藏、专题、观察清单、保存搜索、提醒规则、站内消息。
- 本地词典翻译辅助；PWA 不再展示翻译维护模块，内部维护 API 保留。
- 同一事件规则归并和事件页；保留每条原始来源 URL。
- 本机运行闭环：启动时采集、后台定时采集、运行页状态、手动公开源采集。
- 手机远程骨架：Basic Auth + 用户自行配置的安全通道。
- 行业域热度识别第一期：新闻/事件驱动热门行业域、分数拆解、证据链和手机 PWA 第一屏。
- 行业域详情关联监控股票池 Top 10：按规则关联 A 股监控样本，展示选择理由、命中标签、相关事件和原始证据。
- 页面性能和降噪优化：PWA 按需加载，事件/提醒 compact 列表，行业域推荐改为短期强催化 + 持续热点。
- 版本节点 `1.6`、数据保留说明、SQLite busy timeout/WAL 尝试、当前状态短文档。

未完成：

- B 站采集、OCR、字幕读取、音频转写。
- 微信类真实推送、Webhook 签名和重试。
- 登录态平台适配器。
- 语义检索、向量聚类、长周期事件链追踪。
- 系统服务化、Windows 开机自启、日志轮转、生产级 HTTPS 证书管理。
- 行情、资金流、财务、一致预期、估值和回测等量化确认指标；股票池中的超额收益验证仍为“待验证”。

## 2. 已完成能力索引

| 能力 | 当前状态 | 备注 |
| --- | --- | --- |
| 公开源采集 | 已完成 | RSS/JSON/HTML/RSSHub；失败隔离和源级过滤已覆盖 |
| 搜索增强 | 已完成 | FTS5 优先，LIKE fallback |
| 移动端 PWA | 已完成 MVP | 最新、事件、数据源、搜索、专题、提醒、运行 |
| 手机远程访问 | 已完成骨架 | Basic Auth；安全通道由用户配置 |
| 翻译体验 | 已完成当前边界 | 本地词典辅助，普通 PWA 不展示维护页 |
| RSSHub 适配 | 已完成 MVP | 不并入 RSSHub 代码，不保存凭据 |
| 同事件归并 | 已完成 MVP | 规则归并，非语义聚类 |
| 行业域热度 | 已完成一期 | 短期强催化 + 持续热点；行情、资金、财务尚未接入 |
| 关联监控股票池 | 已完成 MVP | 行业域详情 Top 10；用于监控样本和后续验证 |
| 性能与降噪 | 已完成 MVP | 按需加载、compact 列表、提醒事件级降噪 |
| 节点清理 | 已完成 | 版本 1.6、短上下文、archive 压缩、`.rgignore` |

## 3. 下一阶段建议

### Goal A：行业域行情确认与股票池验证

目标：在现有行业域热度榜和关联监控股票池基础上接入行业指数涨跌幅、相对沪深300收益、成交额占比、上涨家数占比、个股相对行业指数收益等市场确认字段。

边界：

- 不接券商账户或交易接口。
- 市场确认只作为研究辅助，不输出直接买卖建议。
- 没有稳定数据源时显示“未接入/缺失”，不能用新闻热度冒充行情确认。

### Goal B：股票池配置治理与可视化校准

目标：在已有行业域 Top 10 股票池基础上，增加配置校验、人工备注、排序解释和历史分数趋势，帮助后续校准关联度规则。

边界：

- 股票池是监控样本和研究验证对象，不是买入列表。
- 每只股票必须展示关联理由、命中标签、相关事件、证据 URL 和更新时间。
- 初期可先用行业分类、概念标签、业务关键词、新闻/事件共现、来源质量和时间衰减计算关联度。
- 后续再叠加相对行业指数收益、相对沪深300收益、成交额变化、回撤、风险事件和财务/预期指标。
- 不输出买入、卖出、仓位或收益承诺。

### Goal C：行业域资金与产业高频数据

目标：增加 ETF 份额、融资余额、商品价格、库存、开工率、订单、招标等行业域确认信号。

边界：

- 数据源必须公开、可配置、可失败隔离。
- 缺失数据不影响现有新闻/事件热度榜。

### Goal D：本地大模型翻译接入

目标：在不破坏现有词典翻译和原文证据的前提下，新增可选本地大模型翻译 provider。

边界：

- 默认不依赖云 API。
- 未配置模型时系统继续用词典 provider。
- 不把译文覆盖原文、来源 URL 或证据引用。
- PWA 只展示自动翻译结果和失败原因，不恢复手工词典维护工作台。

### Goal E：RSSHub 来源状态诊断

目标：让数据源页或运行页展示每个 RSSHub route 的最近健康、最近采集数量、失败原因和建议处理。

边界：

- 不修改 RSSHub 源码。
- 不依赖真实公网测试通过。
- 401/403/429 时停用或提示，不规避限制。

### Goal F：OCR/字幕/转写接口

目标：在 Extractor 边界内先实现本地 fixture 验证，让 Asset 与 Extraction 可以在详情页展示。

边界：

- 没有外部模型或二进制工具时返回明确未配置状态。
- 单个媒体处理失败不阻断正文内容入库。

### Goal G：站内日报与外部推送准备

目标：在 Notifier 边界内实现站内日报汇总和 Webhook 签名/重试设计。

边界：

- 不直接接入真实微信账号或私密 token。
- 推送内容保留证据引用，不输出直接买卖建议。

### Goal H：大文件模块化重构

目标：降低后续 agent 读取成本，优先拆分 `sqlite.py`、`app.js`、`public_sources.py`。

建议顺序：

1. 把 SQLiteStore 的 API 组装、通知、词典维护拆成小 helper。
2. 把 PWA JS 按页面能力拆成独立静态模块，保持无构建工具。
3. 把公开源 adapter 的 RSSHub/HTML/JSON 解析 helper 拆分，保持 SourceAdapter 接口不变。

## 4. 长期守门规则

- 每轮新增能力必须保留原始 URL 和证据引用。
- 每轮新增外部来源都必须可配置关闭，并证明失败不影响其他来源。
- 不接登录态、Cookie、验证码、JS 渲染、浏览器自动化或反爬绕过来源，除非先单独设计安全边界。
- 不输出直接买卖建议。
- 不硬编码本机绝对路径、真实账号、Cookie、token 或隧道地址。
- 不把 fixture 绿色测试当成真实平台稳定承诺。
